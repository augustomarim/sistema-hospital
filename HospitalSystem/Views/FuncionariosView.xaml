<Window x:Class="HospitalSystem.Views.FuncionariosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Funcionários" Height="650" Width="950"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock Text="Nome" FontWeight="Bold"/>
                    <TextBox x:Name="txtNome" Height="30" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock Text="CPF" FontWeight="Bold"/>
                    <TextBox x:Name="txtCPF" Height="30" Margin="0,4,0,0"
                             MaxLength="14"
                             PreviewTextInput="txtCPF_PreviewTextInput"
                             TextChanged="txtCPF_TextChanged"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="5">
                    <TextBlock Text="Telefone" FontWeight="Bold"/>
                    <TextBox x:Name="txtTelefone" Height="30" Margin="0,4,0,0"
                             MaxLength="15"
                             PreviewTextInput="txtTelefone_PreviewTextInput"
                             TextChanged="txtTelefone_TextChanged"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Margin="5">
                    <TextBlock Text="Tipo" FontWeight="Bold"/>
                    <ComboBox x:Name="cmbTipo" Height="30" Margin="0,4,0,0"
                              SelectionChanged="cmbTipo_SelectionChanged">
                        <ComboBoxItem>Médico</ComboBoxItem>
                        <ComboBoxItem>Enfermeiro</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <Grid Margin="0,10,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock x:Name="lblCrmCoren" Text="CRM/COREN" FontWeight="Bold"/>
                    <TextBox x:Name="txtCrmCoren" Height="30" Margin="0,4,0,0"
                             MaxLength="14"
                             PreviewTextInput="txtCrmCoren_PreviewTextInput"
                             TextChanged="txtCrmCoren_TextChanged"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock x:Name="lblEspecialidadeTurno" Text="Especialidade/Turno" FontWeight="Bold"/>
                    <ComboBox x:Name="cmbEspecialidadeTurno" Height="30" Margin="0,4,0,0"/>
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Salvar" Width="100" Height="35"
                        Margin="5" Click="btnSalvar_Click"/>
                <Button Content="Excluir" Width="100" Height="35"
                        Margin="5" Click="btnExcluir_Click"/>
                <Button Content="Limpar" Width="100" Height="35"
                        Margin="5" Click="btnLimpar_Click"/>
            </StackPanel>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="dgFuncionarios"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="CPF" Binding="{Binding CPF}" Width="130"/>
                <DataGridTextColumn Header="Telefone" Binding="{Binding Telefone}" Width="130"/>
                <DataGridTextColumn Header="CRM/COREN" Binding="{Binding CRM_COREN}" Width="130"/>
                <DataGridTextColumn Header="Especialidade" Binding="{Binding EspecialidadePrincipal}" Width="130"/>
                <DataGridTextColumn Header="Turno" Binding="{Binding Turno}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>