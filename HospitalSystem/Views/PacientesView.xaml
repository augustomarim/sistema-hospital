<Window x:Class="HospitalSystem.Views.PacientesView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Pacientes" Height="600" Width="900">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock Text="Nome" FontWeight="Bold"/>
                    <TextBox x:Name="txtNome" Height="30" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock Text="CPF" FontWeight="Bold"/>
                    <TextBox x:Name="txtCPF" Height="30" Margin="0,4,0,0"
                             MaxLength="14"
                             PreviewTextInput="txtCPF_PreviewTextInput"
                             TextChanged="txtCPF_TextChanged"/> 
                </StackPanel>

                <StackPanel Grid.Column="2" Margin="5">
                    <TextBlock Text="Telefone" FontWeight="Bold"/>
                    <TextBox x:Name="txtTelefone" Height="30" Margin="0,4,0,0"
                             MaxLength="15"
                             PreviewTextInput="txtTelefone_PreviewTextInput"
                             TextChanged="txtTelefone_TextChanged"/>    
                </StackPanel>

                <StackPanel Grid.Column="3" Margin="5">
                    <TextBlock Text="Tipo Sanguíneo" FontWeight="Bold"/>
                    <ComboBox x:Name="cmbTipoSanguineo" Height="30" Margin="0,4,0,0">
                        <ComboBoxItem>A+</ComboBoxItem>
                        <ComboBoxItem>A-</ComboBoxItem>
                        <ComboBoxItem>B+</ComboBoxItem>
                        <ComboBoxItem>B-</ComboBoxItem>
                        <ComboBoxItem>AB+</ComboBoxItem>
                        <ComboBoxItem>AB-</ComboBoxItem>
                        <ComboBoxItem>O+</ComboBoxItem>
                        <ComboBoxItem>O-</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button x:Name="btnSalvar" Content="Salvar" Width="100" Height="35" 
                        Margin="5" Click="btnSalvar_Click"/>
                <Button x:Name="btnExcluir" Content="Excluir" Width="100" Height="35" 
                        Margin="5" Click="btnExcluir_Click"/>
                <Button x:Name="btnLimpar" Content="Limpar" Width="100" Height="35" 
                        Margin="5" Click="btnLimpar_Click"/>
            </StackPanel>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="dgPacientes"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="CPF" Binding="{Binding CPF}" Width="130"/>
                <DataGridTextColumn Header="Telefone" Binding="{Binding Telefone}" Width="120"/>
                <DataGridTextColumn Header="Tipo Sanguíneo" Binding="{Binding TipoSanguineo}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
    
    
</Window>
