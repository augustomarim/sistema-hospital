<Window x:Class="HospitalSystem.Views.DepartamentosView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Departamentos" Height="500" Width="700">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Formulário -->
        <StackPanel Grid.Row="0">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="5">
                    <TextBlock Text="Nome" FontWeight="Bold"/>
                    <TextBox x:Name="txtNome" Height="30" Margin="0,4,0,0"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="5">
                    <TextBlock Text="Bloco" FontWeight="Bold"/>
                    <ComboBox x:Name="cmbBloco" Height="30" Margin="0,4,0,0">
                        <ComboBoxItem>A</ComboBoxItem>
                        <ComboBoxItem>B</ComboBoxItem>
                        <ComboBoxItem>C</ComboBoxItem>
                        <ComboBoxItem>D</ComboBoxItem>
                        <ComboBoxItem>E</ComboBoxItem>
                    </ComboBox>
                </StackPanel>
            </Grid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Salvar" Width="100" Height="35"
                        Margin="5" Click="btnSalvar_Click"/>
                <Button Content="Excluir" Width="100" Height="35"
                        Margin="5" Click="btnExcluir_Click"/>
                <Button Content="Limpar" Width="100" Height="35"
                        Margin="5" Click="btnLimpar_Click"/>
            </StackPanel>
        </StackPanel>

        <!-- Lista -->
        <DataGrid Grid.Row="2" x:Name="dgDepartamentos"
                  AutoGenerateColumns="False"
                  IsReadOnly="True"
                  SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Nome" Binding="{Binding Nome}" Width="*"/>
                <DataGridTextColumn Header="Bloco" Binding="{Binding Bloco}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Window>